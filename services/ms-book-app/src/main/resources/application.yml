server:
  port: 8585
  servlet:
    context-path: /api/v1
spring:
  application:
    name: ms-book-app
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true